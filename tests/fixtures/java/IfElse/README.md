# IfElse

**Covers.** `if / else-if / else` chain returning different values per branch. Exercises the structuring pass's recovery of nested conditionals from Dalvik `if-*` + goto patterns.

**Status.** `compile_pass`. Recompiled program stdout matches `expected.txt` ("pos\nneg\nzero").
