# AbstractMethod

**Covers.** Abstract base class with a concrete subclass overriding the abstract method. Exercises decompilation of body-less methods (`abstract_method` entries with no code_item).

**Status.** `compile_pass`. Recompiled program stdout matches `expected.txt` ("rex:woof").
