# LoopBasic

**Covers.** One `for`-loop with counter init / cond / step, one `while`-loop with a side-effecting body. Exercises the structuring pass's loop-header recovery and back-edge handling for both shapes.

**Status.** `compile_pass`. Recompiled program stdout matches `expected.txt` ("15\n4").
