# SingleMethod

**Covers.** A class exposing exactly one helper method (`square`) plus `main`. Exercises a minimal invoke-static call site and single-expression return.

**Status.** `compile_pass`. Recompiled program stdout matches `expected.txt` ("49").
